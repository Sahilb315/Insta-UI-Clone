import 'package:flutter/material.dart';

class TabReels extends StatefulWidget {
  const TabReels({Key? key}) : super(key: key);

  @override
  State<TabReels> createState() => _TabReelsState();
}

class _TabReelsState extends State<TabReels> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
        centerTitle: true,
      ),
      body: const Center(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}